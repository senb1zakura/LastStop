import 'package:flutter/material.dart';

// Palette
const Color kPrimaryColor = Color(0xFF0B614B); // deep teal
const Color kAccentColor = Color(0xFF00C99A);
const Color kCardColor = Colors.white;

// Background gradient for app shells
const Color kBgGradientStart = Color(0xFFF6FBF9);
const Color kBgGradientEnd = Color(0xFFE8F8F3);

// Reusable layout constants
const BorderRadius kRadius = BorderRadius.all(Radius.circular(16));
const double kPadding = 16.0;

// Text styles
final TextStyle kTitleStyle = TextStyle(
	fontSize: 20,
	fontWeight: FontWeight.w700,
	color: Colors.black87,
);

final TextStyle kSubtitleStyle = TextStyle(
	fontSize: 14,
	color: Colors.black54,
);
